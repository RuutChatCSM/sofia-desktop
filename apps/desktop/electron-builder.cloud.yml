extends: ./electron-builder.base.yml
appId: app.sofia.desktop
productName: Sofia App Cloud
extraMetadata:
  openworkDistribution: cloud
protocols:
  - name: Sofia App Cloud
    schemes:
      - openwork
publish: null
linux:
  executableName: sofia-app-cloud
artifactName: sofia-app-cloud-${os}-${arch}-${version}.${ext}
