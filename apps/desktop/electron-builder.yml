extends: ./electron-builder.base.yml
appId: app.sofia.desktop
productName: Sofia App
protocols:
  - name: Sofia App
    schemes:
      - openwork
      - sofia
linux:
  executableName: sofia-app
artifactName: sofia-app-${os}-${arch}-${version}.${ext}
