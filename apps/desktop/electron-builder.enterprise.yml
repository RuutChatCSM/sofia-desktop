extends: ./electron-builder.base.yml
appId: app.sofia.desktop
productName: Sofia App Enterprise
extraMetadata:
  sofiaDistribution: enterprise
protocols:
  - name: Sofia App Enterprise
    schemes:
      - sofia
publish: null
linux:
  executableName: sofia-app-enterprise
artifactName: sofia-enterprise-${os}-${arch}-${version}.${ext}
