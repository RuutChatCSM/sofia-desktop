apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: sofia-den-allow-eval-mock-egress
spec:
  podSelector:
    matchLabels:
      app.kubernetes.io/name: sofia-ee
      app.kubernetes.io/instance: sofia-ee
    matchExpressions:
      - key: app.kubernetes.io/component
        operator: In
        values:
          - den-api
          - den-web
  policyTypes:
    - Egress
  egress:
    - to:
        - ipBlock:
            cidr: ${SOFIA_EVAL_KUBE_ALLOWED_HOST_IP}/32
      ports:
        - protocol: TCP
          port: ${SOFIA_EVAL_KUBE_ALLOWED_MOCK_PORT}
