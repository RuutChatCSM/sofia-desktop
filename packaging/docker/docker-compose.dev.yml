# docker-compose.dev.yml — Dev testability stack (no custom Dockerfile)
#
# Usage (from repo root):
#   docker compose -f packaging/docker/docker-compose.dev.yml up
#
# Then open the printed Web UI URL — already wired to headless, no config needed.
#
# Env overrides (optional, via .env or export):
#   SOFIA_TOKEN        — shared client token (auto-generated if unset)
#   SOFIA_HOST_TOKEN   — host/admin token (auto-generated if unset)
#   SOFIA_WORKSPACE    — host path to mount as workspace (default: ./workspace)
#   SOFIA_PORT         — host port to map to container :8787 (default: 8787)
#   WEB_PORT              — host port to map to container :5173 (default: 5173)
#   SOFIA_DEV_ID       — unique ID for this stack (default: default)
#   SOFIA_DEV_MODE     — enables isolated OpenCode dev state
#   SOFIA_PUBLIC_HOST  — browser-facing host/IP for LAN-accessible URLs
#   SOFIA_DOCKER_DEV_MOUNT_HOST_OPENCODE=1 — import host OpenCode config/auth into the isolated dev state
#   SOFIA_OPENCODE_CONFIG_DIR      — host OpenCode config dir detection override
#   SOFIA_OPENCODE_DATA_DIR        — host OpenCode data dir detection override
#   SOFIA_HOST_OPENCODE_CONFIG_DIR — import source for OpenCode config
#   SOFIA_HOST_OPENCODE_DATA_DIR   — import source for OpenCode auth/data

x-shared: &shared
  image: node:22-bookworm-slim
  working_dir: /app
  volumes:
    # Mount the entire repo so both services share node_modules + source
    - ../../:/app
    - pnpm-store:/root/.local/share/pnpm/store
    - bun-install:/root/.bun
    - ${SOFIA_WORKSPACE:-./workspace}:/workspace
    - ${SOFIA_HOST_OPENCODE_CONFIG_DIR:-./workspace/.sofia-host-opencode-config}:/persist/.config/opencode:ro
    - ${SOFIA_HOST_OPENCODE_DATA_DIR:-./workspace/.sofia-host-opencode-data}:/persist/.sofia-host-opencode-data:ro

services:
  sofia-server:
    <<: *shared
    entrypoint: ["/bin/sh", "-c"]
    command:
      - |
        set -e

        # --- Ensure workspace dir is writable ---
        mkdir -p /workspace && touch /workspace/.keep 2>/dev/null || true

        # --- Install system deps ---
        apt-get update -qq && apt-get install -y -qq --no-install-recommends \
          curl ca-certificates unzip git >/dev/null 2>&1

        # --- Install bun (cached in named volume) ---
        export BUN_INSTALL="/root/.bun"
        export PATH="$$BUN_INSTALL/bin:$$PATH"
        if ! command -v bun >/dev/null 2>&1; then
          echo "[sofia-server] Installing bun..."
          curl -fsSL https://bun.sh/install | bash
        fi

        # --- Enable pnpm via corepack ---
        corepack enable && corepack prepare pnpm@10.27.0 --activate

        # --- Install deps ---
        echo "[sofia-server] Installing dependencies..."
        pnpm install --no-frozen-lockfile --network-concurrency 1 --child-concurrency 1

        # --- Build the server plugins and install the pinned OpenCode binary ---
        pnpm --filter sofia-server build
        OPENCODE_VERSION=$$(node -e 'const fs=require("fs"); const parsed=JSON.parse(fs.readFileSync("/app/constants.json", "utf8")); process.stdout.write(String(parsed.opencodeVersion || "").trim().replace(/^v/, ""));')
        export OPENCODE_INSTALL_DIR="/root/.opencode/bin"
        mkdir -p "$$OPENCODE_INSTALL_DIR"
        if ! command -v opencode >/dev/null 2>&1; then
          curl -fsSL https://opencode.ai/install | bash -s -- --version "$$OPENCODE_VERSION" --no-modify-path
        fi
        export PATH="$$OPENCODE_INSTALL_DIR:$$PATH"
        export SOFIA_MANAGE_OPENCODE=1
        export SOFIA_OPENCODE_BIN="$$(command -v opencode)"
        export SOFIA_EXTENSIONS_PLUGIN_DIR="/app/apps/server/dist/opencode-plugins"

        # --- Resolve tokens ---
        if [ -z "$$SOFIA_TOKEN" ]; then
          SOFIA_TOKEN=$$(cat /proc/sys/kernel/random/uuid)
          export SOFIA_TOKEN
        fi
        if [ -z "$$SOFIA_HOST_TOKEN" ]; then
          SOFIA_HOST_TOKEN=$$(cat /proc/sys/kernel/random/uuid)
          export SOFIA_HOST_TOKEN
        fi

        # Write tokens so the web service can source them
        mkdir -p /app/tmp
        printf 'SOFIA_TOKEN=%s\nSOFIA_HOST_TOKEN=%s\n' \
          "$$SOFIA_TOKEN" "$$SOFIA_HOST_TOKEN" > "/app/tmp/.dev-env-${SOFIA_DEV_ID:-default}"

        echo ""
        echo "============================================"
        echo "  Sofia server"
        echo "  Server:     http://localhost:${SOFIA_PORT:-8787}"
        echo "  Health:     http://localhost:${SOFIA_PORT:-8787}/health"
        echo "  Token:      $$SOFIA_TOKEN"
        echo "  Host token: $$SOFIA_HOST_TOKEN"
        echo "============================================"
        echo ""

        exec pnpm --filter sofia-server exec bun dist/cli.js \
          --workspace /workspace \
          --host 0.0.0.0 \
          --port 8787 \
          --token "$$SOFIA_TOKEN" \
          --host-token "$$SOFIA_HOST_TOKEN" \
          --approval auto \
          --cors "*" \
          --verbose
    ports:
      - "${SOFIA_PORT:-8787}:8787"
    healthcheck:
      test: ["CMD-SHELL", "curl -sf http://localhost:8787/health || exit 1"]
      interval: 5s
      timeout: 5s
      retries: 30
      start_period: 90s
    environment:
      CI: "true"
      SOFIA_TOKEN: ${SOFIA_TOKEN:-}
      SOFIA_HOST_TOKEN: ${SOFIA_HOST_TOKEN:-}
      SOFIA_DEV_ID: ${SOFIA_DEV_ID:-default}
      SOFIA_DEV_MODE: ${SOFIA_DEV_MODE:-1}
      SOFIA_DEV_OPENCODE_IMPORT_CONFIG_DIR: /persist/.config/opencode
      SOFIA_DEV_OPENCODE_IMPORT_DATA_DIR: /persist/.sofia-host-opencode-data

  web:
    <<: *shared
    depends_on:
      sofia-server:
        condition: service_healthy
    entrypoint: ["/bin/sh", "-c"]
    command:
      - |
        set -e

        # --- Install system deps ---
        apt-get update -qq && apt-get install -y -qq --no-install-recommends \
          curl ca-certificates >/dev/null 2>&1

        # --- Bun + pnpm ---
        export BUN_INSTALL="/root/.bun"
        export PATH="$$BUN_INSTALL/bin:$$PATH"
        if ! command -v bun >/dev/null 2>&1; then
          curl -fsSL https://bun.sh/install | bash
        fi
        corepack enable && corepack prepare pnpm@10.27.0 --activate

        # --- Read token written by sofia-server ---
        if [ -f "/app/tmp/.dev-env-${SOFIA_DEV_ID:-default}" ]; then
          . "/app/tmp/.dev-env-${SOFIA_DEV_ID:-default}"
          export VITE_SOFIA_TOKEN="$$SOFIA_TOKEN"
        fi

        echo ""
        echo "============================================"
        echo "  Sofia web UI"
        echo "  URL:   http://localhost:${WEB_PORT:-5173}"
          echo "  Token: $${VITE_SOFIA_TOKEN:-<see sofia-server logs>}"
        echo "============================================"
        echo ""

        export VITE_SOFIA_URL="http://${SOFIA_PUBLIC_HOST:-localhost}:${SOFIA_PORT:-8787}"
        export VITE_SOFIA_PORT="${SOFIA_PORT:-8787}"
        export VITE_ALLOWED_HOSTS="all"
        export HOST="0.0.0.0"
        export PORT="5173"

        exec pnpm --filter @sofia/app exec vite \
          --host 0.0.0.0 \
          --port 5173 \
          --strictPort
    ports:
      - "${WEB_PORT:-5173}:5173"
    environment:
      SOFIA_DEV_ID: ${SOFIA_DEV_ID:-default}

volumes:
  pnpm-store:
    name: sofia-dev-pnpm-store
  bun-install:
    name: sofia-dev-bun-install
