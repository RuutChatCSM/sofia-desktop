# The builder runs on the BUILD host's native platform and cross-compiles for
# the target: bun compiles sofia-server via --target and the web build is
# arch-independent. This avoids emulating the whole toolchain under QEMU
# (node's libuv crashes under qemu-x86_64: uv__io_poll EEXIST assert).
FROM --platform=$BUILDPLATFORM node:22-bookworm-slim AS sofia-builder

WORKDIR /src

RUN apt-get update \
  && apt-get install -y --no-install-recommends ca-certificates curl git unzip \
    python3 make g++ \
  && npm install -g bun \
  && corepack enable \
  && rm -rf /var/lib/apt/lists/*

COPY . .

# python3/make/g++ above: better-sqlite3 v13 has no install script, but pnpm's
# implicit node-gyp build for packages with a binding.gyp still compiles it
# from source here, which needs a toolchain (v12 downloaded prebuilds instead).
RUN pnpm install --frozen-lockfile --filter sofia-server... --filter @sofia/app...

ARG TARGETARCH

RUN set -eux; \
  arch="${TARGETARCH:-$(dpkg --print-architecture)}"; \
  case "$arch" in \
    amd64) target="bun-linux-x64" ;; \
    arm64) target="bun-linux-arm64" ;; \
    *) echo "unsupported architecture: $arch" >&2; exit 1 ;; \
  esac; \
  pnpm --filter sofia-server build; \
  pnpm --filter sofia-server exec bun ./script/build.ts --outdir dist/bin --target "$target"; \
  install -m 0755 "apps/server/dist/bin/sofia-server-${target}" /tmp/sofia-server; \
  pnpm --filter @sofia/app build:web

FROM node:22-bookworm-slim

ARG SOFIA_SERVER_VERSION=0.18.1
ARG SOFIA_ENGINE_VERSION
ARG SOFIA_ENGINE_DOWNLOAD_URL=

RUN apt-get update \
  && apt-get install -y --no-install-recommends ca-certificates curl tar unzip \
  && rm -rf /var/lib/apt/lists/*

COPY --from=sofia-builder /tmp/sofia-server /usr/local/bin/sofia-server
COPY --from=sofia-builder /src/apps/app/dist /opt/sofia/web
COPY --from=sofia-builder /src/apps/server/dist/engine-plugins /opt/sofia/engine-plugins

RUN set -eux; \
  test -n "$SOFIA_ENGINE_VERSION"; \
  arch="$(dpkg --print-architecture)"; \
  case "$arch" in \
    amd64) asset="engine-linux-x64-baseline.tar.gz" ;; \
    arm64) asset="engine-linux-arm64.tar.gz" ;; \
    *) echo "unsupported architecture: $arch" >&2; exit 1 ;; \
  esac; \
  url="$SOFIA_ENGINE_DOWNLOAD_URL"; \
  if [ -z "$url" ]; then \
    url="https://github.com/RuutChatCSM/sofia/releases/download/v${SOFIA_ENGINE_VERSION}/${asset}"; \
  fi; \
  tmpdir="$(mktemp -d)"; \
  curl -fsSL "$url" -o "$tmpdir/$asset"; \
  tar -xzf "$tmpdir/$asset" -C "$tmpdir"; \
  binary="$(find "$tmpdir" -type f -name engine | head -n 1)"; \
  test -n "$binary"; \
  install -m 0755 "$binary" /usr/local/bin/engine; \
  rm -rf "$tmpdir"

# Runtime asserts run the target-arch binaries. Keep them on in CI (native
# amd64 runners); disable only for local cross-builds, where bun binaries die
# with "Illegal instruction" under qemu-x86_64 even though they run fine on
# real hardware.
ARG RUNTIME_ASSERTS=1
RUN if [ "$RUNTIME_ASSERTS" = "1" ]; then \
    test -f /opt/sofia/engine-plugins/sofia-extensions-preview.js \
    && test -f /opt/sofia/engine-plugins/sofia-capabilities-knowledge.js \
    && test -f /opt/sofia/engine-plugins/sofia-office-attachments.js \
    && test -f /opt/sofia/engine-plugins/sofia-anthropic-adaptive-thinking.js \
    && test -f /opt/sofia/engine-plugins/sofia-anthropic-tool-schema.js \
    && test "$(sofia-server --version)" = "$SOFIA_SERVER_VERSION" \
    && engine --version; \
  else \
    echo "RUNTIME_ASSERTS disabled (cross-build); binaries must be verified on target hardware"; \
  fi

CMD ["sleep", "infinity"]
